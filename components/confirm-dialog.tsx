type ConfirmDialogProps = {
  trigger: React.ReactNode;
  action: string;
};

const ConfirmDialog = ({ trigger, action }: ConfirmDialogProps) => {
  return <>{trigger}</>;
};

export { ConfirmDialog };
